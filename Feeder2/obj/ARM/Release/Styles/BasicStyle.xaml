<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Feeder.PivotApp.Styles">

    <Style TargetType="TextBlock" x:Key="Title">
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="Status">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Margin" Value="0 0 20 0"/>
        <Setter Property="FontSize" Value="{ThemeResource TextStyleMediumFontSize}"/>
        <Setter Property="Foreground" Value="Gray"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ItemTitle">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="ItemSubtitle">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="Gray"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="ItemsCountTextBlock" TargetType="TextBlock" BasedOn="{StaticResource ListViewItemTextBlockStyle}">
        <Setter Property="FontSize" Value="{ThemeResource TextStyleLargeFontSize}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="FolderItemListSub" TargetType="TextBlock" BasedOn="{StaticResource ListViewItemContentTextBlockStyle}">
        <Setter Property="Foreground" Value="SteelBlue"/>
    </Style>

    <Style x:Key="FolderItemList" TargetType="TextBlock" BasedOn="{StaticResource ListViewItemTextBlockStyle}">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="Category">
        <Setter Property="FontSize" Value="{ThemeResource TextStyleExtraLargeFontSize}"/>
        <Setter Property="Foreground" Value="GreenYellow"/>
        <Setter Property="Margin" Value="10 0 0 0"/>
    </Style>

</ResourceDictionary>

